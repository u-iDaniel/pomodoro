{
  "name": "this package json is only for heroku cuz its sassy",
  "private": true,
  "scripts": {
    "heroku-prebuild": "npm install --prefix web",
    "heroku-postbuild": "npm run build --prefix web",
    "start": "npm start --prefix web"
  }
}